# CeloChess Client Environment Variables
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local or .env files to version control!

# ============================================
# PUBLIC VARIABLES (Exposed to client bundle)
# ============================================
# ⚠️ WARNING: Variables prefixed with VITE_PUBLIC_ are exposed in the client-side bundle
# NEVER put secrets, private keys, or sensitive data in VITE_PUBLIC_ variables!

# WalletConnect Project ID
# Get your project ID from https://cloud.reown.com
# This is safe to expose as it's a public identifier
VITE_WALLET_CONNECT_PROJECT_ID=your_walletconnect_project_id_here

# Contract addresses (public information)
VITE_MATCH_MANAGER_BASE=0x0000000000000000000000000000000000000000
VITE_MATCH_MANAGER_CELO=0x0000000000000000000000000000000000000000
VITE_MATCH_MANAGER_CELO_SEPOLIA=0x0000000000000000000000000000000000000000

# ============================================
# SECURITY NOTES
# ============================================
# ❌ NEVER use VITE_PUBLIC_ prefix for:
#   - Private keys
#   - API keys with write permissions
#   - Database credentials
#   - Any sensitive authentication tokens
#
# ✅ Safe to use VITE_PUBLIC_ for:
#   - Public contract addresses
#   - Public RPC URLs
#   - Public API endpoints
#   - Non-sensitive configuration values
#
# For server-side secrets, use environment variables WITHOUT the VITE_PUBLIC_ prefix
# and access them only in server-side code (Node.js scripts, API routes, etc.)
